name: Svelte Continous Integration On GitHub
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  pull_request:
    brances: [main]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: node env
        uses: actions/setup-node@v3
